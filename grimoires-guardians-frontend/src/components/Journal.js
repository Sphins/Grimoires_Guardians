import React from 'react';
import { Box, Typography } from '@mui/material';

const Journals = () => {
    return (
        <Box>
            <Typography variant="h5" gutterBottom>Journaux</Typography>
            {/* Contenu des journaux */}
        </Box>
    );
};

export default Journals;
