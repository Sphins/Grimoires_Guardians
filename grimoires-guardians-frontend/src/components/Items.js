import React from 'react';
import { Box, Typography } from '@mui/material';

const Items = () => {
    return (
        <Box>
            <Typography variant="h5" gutterBottom>Objets</Typography>
            {/* Contenu des objets */}
        </Box>
    );
};

export default Items;
